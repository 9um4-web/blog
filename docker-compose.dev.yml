services:
  db:
    image: postgres:17-alpine
    container_name: blog-dev-db
    environment:
      POSTGRES_USER: blog
      POSTGRES_PASSWORD: blog
      POSTGRES_DB: blog
    ports:
      - "5432:5432"
    volumes:
      - blog_dev_pgdata:/var/lib/postgresql/data

volumes:
  blog_dev_pgdata:
